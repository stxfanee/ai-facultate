# Client usage

Client Mode connects to an existing public or LAN server URL.

The client:

- opens the AI interface inside the desktop app;
- stores cookies/session data between launches;
- does not start local AI services;
- does not download models;
- does not expose server settings unless Developer Mode is enabled.

If the server is unavailable, the app shows a friendly retry/settings screen instead of raw technical errors.
